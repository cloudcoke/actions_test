name: Actions Events

on:
    push:
        branches: main

jobs:
    evnets:
        runs-on: ubuntu-latest
        steps:
            - name: workflow_run
              run: |
                  echo ${{github.evnet}}
