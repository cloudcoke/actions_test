name: Actions Payloads

on:
    push:
        branches: main

jobs:
    evnets:
        runs-on: ubuntu-latest
        steps:
            - name: Payloads
              env:
                  GITHUB_CONTEXT: ${{ toJson(github) }}
              run: |
                  echo "$GITHUB_CONTEXT"
