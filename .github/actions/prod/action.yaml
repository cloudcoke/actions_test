name: Prod Deploy

inputs:
    environment:
        required: true

runs:
    using: composite

    steps:
        - name: List Files
          run: ls
          shell: bash

        - name: Dynamic Secrets
          env:
              MY_SECRET: ${{ secrets[format('{0}_A', inputs.environment)] }}
          run: echo $MY_SECRET'
          shell: bash
